type DockerNetwork {
    id: ID!
    name: String!
    labels: [KeyValue!]!
    created: DateTime!
    ipv6: Boolean!
    driver: String!
    internal: Boolean!
    subnet: String
    gateway: String
    containers: [DockerContainer!]!
}
