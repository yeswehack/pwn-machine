extend type Query {
  dockerVolumes: [DockerVolume!]!
}

type DockerVolume {
  name: String!
  labels: [KeyValue!]!
  created: DateTime!
  mountpoint: String!
  usingContainers(onlyRunning: Boolean = true): [DockerContainer!]!
}
