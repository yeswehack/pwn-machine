type Cursor {
    from: Int!
    size: Int!
}
input CursorInput {
    from: Int!
    size: Int!
}

interface Node{
    nodeId: ID!
}

interface Paginated {
    next: Cursor
    prev: Cursor
    total: Int!
    result: [Node!]
}