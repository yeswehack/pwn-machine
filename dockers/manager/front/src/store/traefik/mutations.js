
export function setEntrypoints(state, entrypoints) {
    state.entrypoints = entrypoints
}

export function setRouters(state, routers) {
    state.routers = routers
}

export function setServices(state, services) {
    state.services = services
}

export function setMiddlewares(state, middlewares) {
    state.middlewares = middlewares
}
