fragment NetworkFragment on DockerNetwork {
  id
  name
  labels {
    key
    value
  }
  builtin
  created
  ipv6
  driver
  internal
  ipams {
    subnet
    ipRange
    gateway
  }
  connections {
    ipv4Address
    ipv6Address
    container{
      name
      id
    }
  }
}
