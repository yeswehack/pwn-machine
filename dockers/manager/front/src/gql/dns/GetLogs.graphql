query getDnsLogs($filter: DnsLogFilter, $cursor: CursorInput) {
  dnsLogs(filter: $filter, cursor: $cursor) {
    nodeId
    origin
    date
    query
    type
    error
    responses {
      name
      rdata
      type
    }
  }
}
