mutation updateDnsRule($nodeId: ID!, $patch: UpdateDnsRuleInput!) {
  updateDnsRule(nodeId: $nodeId, patch: $patch) {
    nodeId
    zone
    name
    type
    records {
      content
      enabled
    }
  }
}
