mutation updateDnsZone($nodeId: ID!, $patch: UpdateDnsZoneInput!) {
  updateDnsZone(nodeId: $nodeId, patch: $patch) {
    name
    serial
    nodeId
    soa {
      nameserver
      postmaster
      refresh
      retry
      expire
      ttl
    }
  }
}
