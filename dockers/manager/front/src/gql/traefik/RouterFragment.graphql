fragment RouterFragment on TraefikRouter {
  name
  entryPoints
  service {
    name
    enabled
    loadBalancer {
      healthCheck {
        followRedirects
        path
        headers {
          name
          value
        }
      }
    }
  }
  middlewares {
    name
  }
  priority
  rule
  enabled
  protocol
  provider
}
