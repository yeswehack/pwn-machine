fragment ImageFragment on DockerImage {
  id
  name
  tags {
    repository
    tag
  }
  labels {
    key
    value
  }
  parent
  created
  size
  entrypoint
  command
  environment {
    key
    value
  }
}
